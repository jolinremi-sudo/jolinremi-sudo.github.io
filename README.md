# Creative Portfolio

A minimal Next.js project with TypeScript and Tailwind CSS.

## Run locally

1. Open a terminal in this folder.
2. Run `pnpm install` once to download dependencies.
3. Run `pnpm dev`.
4. Open `http://localhost:3000` in your browser.
